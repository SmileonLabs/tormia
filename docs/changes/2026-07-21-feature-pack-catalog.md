# Opt-in feature pack catalog

Added a project-owned `OntologyFeaturePack` and catalog. A pack groups
canonical concepts, authored fact templates, rule IDs, rule-block presets,
action references, and physical profile references. The catalog is opt-in and
does not activate a rule merely because a pack exists; activation remains an
explicit profile/world operation.

