# 트리플 편집기 canonical 변환

런타임 월드 편집기는 행 UI뿐 아니라 컨트롤러 경계에서도 관계와 대상 값을
canonical ID로 변환합니다. 따라서 UI가 아닌 호출 경로로 트리플을 작성해도
한글 표시명이 저장되지 않습니다.

하이라키 기반 패널은 화면과 행 콜백을 담당하고, 컨트롤러는 canonical
온톨로지 데이터를 담당합니다. 이번 변경에서는 레이아웃이나 프리팹 값을
코드로 생성하지 않습니다.

