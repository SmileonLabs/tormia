# Triple editor canonicalization

The runtime world editor now canonicalizes relation and object values at the
controller boundary as well as in the row UI. This prevents localized labels
from being persisted when a triple is authored through a non-UI caller.

The hierarchy-authored panel remains responsible for presentation and row
callbacks; the controller owns canonical ontology data. No layout or prefab
values are generated by this change.

