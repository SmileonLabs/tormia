# Save migration for rule IDs

Save migration now canonicalizes rule IDs in placed-object bindings, semantic
contribution ledgers, and the world controlled-rule list. Derived runtime facts
remain excluded from snapshots. Feature Pack catalog assets remain design-time
catalog data until an explicit activation command is introduced.

