# Result origin labels

The selected-object Results tab now labels current facts as Authored,
Inferred, or Observed. Authored values come from the selected `OntologyObject`,
retractable rule effects are classified as Inferred, and remaining runtime
values are presented as Observed. The labels are localized through the existing
English/Korean CSV packs; canonical Fact IDs remain unchanged.

