# Manifest 기반 공격 재생 속도

무기는 `attack_playback_speed` Triple을 작성하고 배정된 휘두르기 Rule Block이
이를 표현 속도 출처로 선언합니다. Authority가 평가한 값을 `AttackLight`와 함께
전달하며 Manifest는 `1.0x`를 유지합니다. Unity는 승인 배율만 적용하고 Triple이나
Rule Block이 없으면 이름 기반 대체 없이 실패합니다.
